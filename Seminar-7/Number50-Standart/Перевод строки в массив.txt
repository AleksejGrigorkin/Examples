string[] newNumbers = numbers.Split(new char[] { ',' }, StringSplitOptions.RemoveEmptyEntries)

int k = 0;
for (int i = 0; i < newNumbers.Length; i++)
{
  if (numbers[i] == ',')
  {
    k++;
  }
  else
  {
    newNumbers[k] = newNumbers[k]+$"{numbers[i]}";
  }
}